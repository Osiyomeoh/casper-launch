[workspace]
members = [
    "rwa-nft",
    "yield-distributor",
    "governance",
    "trade-escrow",
    "tests",
]
resolver = "2"

[profile.release]
codegen-units = 1
lto = true
opt-level = "z"
panic = "abort"
strip = "symbols"
